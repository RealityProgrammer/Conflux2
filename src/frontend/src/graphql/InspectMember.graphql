query InspectMember($id: UUID!) {
    communityServerMemberForAdmin(id: $id) {
        id
        createdAt
        user {
            id
            userName
            displayName
            hasAvatar
        }
        roles {
            id
            name
            authorizeLevel
            specialRoleType
        }
        authorizeInfo {
            authorizeLevel
            permissions {
                permission
                isGranted
            }
            isBanned
        }
        status
        banExpireAt
    }
}