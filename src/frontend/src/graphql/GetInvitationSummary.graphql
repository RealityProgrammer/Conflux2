query GetInvitationSummary($id: String!) {
    invitationById(id: $id) {
        communityServer {
            id
            name,
            hasAvatar
            numMembers
        },
        status
    }
}