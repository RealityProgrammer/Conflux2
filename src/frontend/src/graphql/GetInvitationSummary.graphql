query GetInvitationSummary($id: String!) {
    invitation(id: $id) {
        communityServer {
            id
            name,
            hasAvatar
            numMembers
        },
        status
    }
}