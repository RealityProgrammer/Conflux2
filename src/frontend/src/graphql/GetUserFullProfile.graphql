query GetUserFullProfile($id: UUID!) {
    user(id: $id) {
        id,
        userName,
        displayName,
        hasAvatar,
        biography,
        pronouns,
        createdAt,
        numMutualFriends
    }
}