query GetUserFullProfile($id: UUID!) {
    user(id: $id) {
        id,
        userName,
        displayName,
        avatarRevision,
        biography,
        pronouns,
        createdAt,
        numMutualFriends
    }
}