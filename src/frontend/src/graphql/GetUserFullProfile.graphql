query GetUserFullProfile($id: UUID!) {
    userById(id: $id) {
        id,
        userName,
        displayName,
        hasAvatar,
        biography,
        pronouns,
        createdAt,
        numMutualFriends
    }
}