query GetUserFullProfile($id: UUID!) {
    user(id: $id) {
        id
        userName
        displayName
        avatarRevision
        bannerRevision
        biography
        pronouns
        createdAt
        numMutualFriends
        effectivePresenceStatus
    }
}