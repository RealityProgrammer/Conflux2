query GetSessionUserProfileSettingInfo {
    sessionUser {
        id,
        userName,
        displayName,
        hasAvatar,
        biography,
        pronouns,
        createdAt,
        manualPresenceStatus,
    }
}