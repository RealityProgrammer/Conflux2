query GetSessionUserProfileSettingInfo {
    sessionUser {
        id,
        userName,
        displayName,
        hasAvatar,
        hasBanner,
        biography,
        pronouns,
        createdAt,
        manualPresenceStatus,
    }
}