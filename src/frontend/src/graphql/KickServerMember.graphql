mutation KickServerMember($serverId: UUID!, $memberId: UUID!, $reason: String) {
    kickCommunityServerMember(input: {
        serverId: $serverId
        memberId: $memberId
        reason: $reason
    }) {
        memberId
    }
}