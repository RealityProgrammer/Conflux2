mutation BanServerMember($serverId: UUID!, $memberId: UUID!, $reason: String, $duration: Duration) {
    banCommunityServerMember(input: {
        serverId: $serverId,
        memberId: $memberId,
        reason: $reason,
        duration: $duration
    }) {
        memberId
    }
}