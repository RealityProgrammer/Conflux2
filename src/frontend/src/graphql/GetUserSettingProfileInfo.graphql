query GetUserSettingProfileInfo($id: UUID!) {
    user(id: $id) {
        id,
        userName,
        displayName,
        hasAvatar,
        biography,
        pronouns,
        createdAt,
        manualPresenceStatus,
    }
}